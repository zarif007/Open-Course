import { ITopicVersion } from '@/types/topicVersion';
import React from 'react';

const DisplayVersions = ({ versions }: { versions: ITopicVersion[] }) => {
  return <div>DisplayVersions</div>;
};

export default DisplayVersions;
